{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd bot && npm ci --omit=dev"
  },
  "deploy": {
    "startCommand": "cd bot && FINCH_SKILL_PATH=$PWD/../SKILL.md FINCH_SPEC_PATH=$PWD/../bazantic/openapi.json npm run serve:prod",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "numReplicas": 1
  }
}
