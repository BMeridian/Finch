{
  "name": "finch-subgraph",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:goldsky": "goldsky subgraph deploy finch-live/0.3.0 --path .",
    "create-local": "graph create --node http://localhost:8020/ finch",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 finch"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.71.2",
    "@graphprotocol/graph-ts": "0.35.1"
  }
}
